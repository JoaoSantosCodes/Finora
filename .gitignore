node_modules
dist
dist-ssr
*.tsbuildinfo
*.local
.DS_Store
.env
.env.*
!.env.example

# Supabase CLI — artefatos locais (não versionados). Migrations/seed/config SÃO versionados.
supabase/.temp/
supabase/.branches/

# Segredos de credenciais que nunca devem ser versionados
client_secret_*.json
*service-account*.json
*.pem
*.key

# Ignore local Windows Junction / Symlink folder App (raiz apenas — não deve casar com android/app/ etc.)
/App/

