# apps/

Aplicações finais do Finora.

- `web/` — Aplicação Web/PWA (React + Vite). O V0 atual vive na raiz do repositório e será migrado para cá a partir da tarefa WEB-001, mantendo-se funcional durante a transição.
- `mobile/` — App Android (React Native), fase V1.5, reaproveitando `packages/core` e `packages/api-client`.
